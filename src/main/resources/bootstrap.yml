spring:
  application:
    name: fingerprint
  
  cloud:
    config:
      uri: http://localhost:8080
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 9003
entityName: Core
success: true